# Project Guidelines: 자율 완결형 작업 원칙 (Autonomous End-to-End Execution)

이 프로젝트에서는 사용자가 중간 단계마다 승인(Submit)을 누르는 번거로움을 최소화하기 위해 **완결형 자율 에이전트(Autonomous Mode)** 방식으로 동작합니다.

## 1. 무중단 자율 완결 (Zero Unnecessary Pauses)
- 작업 요청을 받으면 중간 단계마다 사용자 승인을 요구하거나 작업을 중단하지 않습니다.
- "진행할까요?", "다음 파일을 수정할까요?"와 같은 확인성 중간 질문을 하지 않고, 분석 → 수정 → 빌드 → 테스트/검증 단계까지 한 번의 실행 사이클로 끝마칩니다.
- 사용자가 명시적으로 단계별 확인을 요청하지 않는 한, 기획안 확인을 위한 대기 상태를 만들지 않고 곧바로 최종 결과물까지 완성합니다.

## 2. 자동 빌드 및 자가 검증 (Mandatory Build & Self-Testing)
- 코드 수정 후에는 반드시 자체적으로 빌드 명령어(`npm run build`)를 실행하여 번들링 및 타입/문법 오류를 검증합니다.
- 빌드 에러나 경고가 발생하면 사용자에게 보고하기 전에 스스로 에러 로그를 분석하여 수정하고, 빌드가 깨끗하게 성공할 때까지 반복 수정합니다.
- 웹 UI 및 컴포넌트 추가/수정 시 브라우저 서브에이전트 또는 자체 검증 스크립트로 동작을 최종 점검합니다.

## 3. 원격 배포 필수 완결 (Mandatory Production Deployment)
- 사용자가 "로컬로만 보자" 또는 "로컬에서만 확인하겠다"고 명시하지 않는 한, 모든 작업 완료 시 반드시 변경 사항을 Git 커밋 및 원격 Push하고, 프로덕션 배포(`npm run deploy` 등)까지 직접 완결합니다.
- `main` 브랜치 Push(Vercel 등 CI/CD 연동) 및 `npm run deploy`(GitHub Pages 배포)를 모두 수행하여 실제 배포 환경에 즉각 반영되도록 합니다.
- 배포 완료 후 최종 제공된 배포 URL을 함께 안내합니다.

## 4. 선제적 합리적 결정 (Proactive Decision Making)
- 세부 옵션이나 사소한 기획적 모호성이 있더라도, 기존 코드 컨벤션과 프로젝트 의도에 가장 부합하는 최선의 방식으로 선제적 구현을 완료합니다.
- 선택한 사유와 근거는 모든 검증이 끝난 후 최종 완료 보고에서 설명합니다.

## 5. 최종 완료 보고 (One-Stop Final Summary)
- 모든 구현, 빌드 통과, 검증 및 원격 배포가 완료된 시점에만 요약된 최종 결과를 사용자에게 안내합니다.
