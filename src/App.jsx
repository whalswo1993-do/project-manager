import { useEffect,useMemo,useState } from "react";
import "./App.css";
import { supabase } from "./supabase";
import Login from "./Login";

const DAY=86400000;
const DEPTS=["PM","설계","설비기술","제어","비전"];
const STATUSES=["검토중","PO대기중","제작 및 운송중","진행중","완료"];
const iso=(d=new Date())=>d.toISOString().slice(0,10),parse=s=>new Date(`${s}T00:00:00`),uid=()=>`${Date.now()}-${Math.random().toString(16).slice(2)}`;
const rows5=()=>Array.from({length:5},()=>({id:uid(),name:"",date:iso()}));
const blank=()=>({manufacturingNo:"",site:"",name:"",pm:"",design:"",facilityTechnology:"",control:"",vision:"",startDate:iso(),endDate:iso(new Date(Date.now()+7*DAY)),status:"검토중",progress:0,autoProgress:true,milestones:[]});
const toDb=p=>({id:p.id,manufacturing_no:p.manufacturingNo.trim(),site:p.site,name:p.name.trim(),manager:p.pm.trim(),pm:p.pm.trim(),design:p.design.trim(),facility_technology:p.facilityTechnology.trim(),control:p.control.trim(),vision:p.vision.trim(),start_date:p.startDate,end_date:p.endDate,status:p.status,progress:+p.progress||0,auto_progress:p.autoProgress,milestones:p.milestones||[]});
const fromDb=p=>({id:p.id,manufacturingNo:p.manufacturing_no||"",site:p.site||"",name:p.name||"",pm:p.pm||p.manager||"",design:p.design||"",facilityTechnology:p.facility_technology||"",control:p.control||"",vision:p.vision||"",startDate:p.start_date,endDate:p.end_date,status:p.status||"검토중",progress:p.progress||0,autoProgress:p.auto_progress??true,milestones:p.milestones||[]});
const progressOf=p=>p.status==="완료"?100:!p.autoProgress?+p.progress||0:Math.max(0,Math.min(100,Math.round((parse(iso())-parse(p.startDate))/Math.max(DAY,parse(p.endDate)-parse(p.startDate))*100)));

export default function App(){
 const [session,setSession]=useState(null),[loading,setLoading]=useState(true),[profile,setProfile]=useState(null),[users,setUsers]=useState([]),[projects,setProjects]=useState([]),[sites,setSites]=useState([]),[personnel,setPersonnel]=useState([]);
 const [form,setForm]=useState(blank()),[editing,setEditing]=useState(null),[filter,setFilter]=useState("전체"),[search,setSearch]=useState(""),[message,setMessage]=useState(""),[modal,setModal]=useState(null);
 const [newSite,setNewSite]=useState(""),[newPerson,setNewPerson]=useState(""),[newDept,setNewDept]=useState("PM"),[milestones,setMilestones]=useState(rows5());
 useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setLoading(false)});const {data}=supabase.auth.onAuthStateChange((_,s)=>setSession(s));return()=>data.subscription.unsubscribe()},[]);
 useEffect(()=>{if(session)boot();else{setProfile(null);setProjects([])}},[session]);
 async function boot(){const {data:p,error}=await supabase.from("profiles").select("*").eq("id",session.user.id).single();if(error)return setMessage(error.message);setProfile(p);await Promise.all([loadProjects(),loadSites(),loadPeople()]);if(p.role==="admin")await loadUsers()}
 async function loadProjects(){const {data,error}=await supabase.from("projects").select("*").order("created_at",{ascending:false});if(error)setMessage(error.message);else setProjects((data||[]).map(fromDb))}
 async function loadSites(){const {data,error}=await supabase.from("sites").select("*").order("name");if(error)setMessage(error.message);else setSites(data||[])}
 async function loadPeople(){const {data,error}=await supabase.from("personnel").select("*").order("department").order("name");if(error)setMessage(error.message);else setPersonnel(data||[])}
 async function loadUsers(){const {data,error}=await supabase.from("profiles").select("*").order("email");if(error)setMessage(error.message);else setUsers(data||[])}
 const role=profile?.role,canCreate=["admin","grade3"].includes(role),canEdit=["admin","grade3","grade2"].includes(role),canDelete=["admin","grade3"].includes(role);
 const enriched=useMemo(()=>projects.map(p=>({...p,value:progressOf(p),overdue:p.status!=="완료"&&parse(p.endDate)<parse(iso())})),[projects]);
 const visible=enriched.filter(p=>[p.manufacturingNo,p.site,p.name,p.pm,p.design,p.facilityTechnology,p.control,p.vision].join(" ").toLowerCase().includes(search.toLowerCase())&&(filter==="전체"||(filter==="완료"&&p.status==="완료")||(filter==="진행중"&&p.status!=="완료")||(filter==="지연"&&p.overdue)));
 const peopleFor=d=>personnel.filter(p=>p.department===d);
 async function addSite(){const name=newSite.trim();if(!name)return setMessage("Site명을 입력하세요.");const {error}=await supabase.from("sites").insert({name,created_by:session.user.id});if(error)return setMessage(error.code==="23505"?"이미 등록된 Site입니다.":error.message);setNewSite("");await loadSites()}
 async function addPerson(){const name=newPerson.trim();if(!name)return setMessage("담당자 이름을 입력하세요.");const {error}=await supabase.from("personnel").insert({name,department:newDept,created_by:session.user.id});if(error)return setMessage(error.code==="23505"?"이미 등록된 담당자입니다.":error.message);setNewPerson("");await loadPeople()}
 async function deleteSite(e,id,name){e.preventDefault();if(!confirm(`Site '${name}'을 삭제할까요?\n기존 프로젝트에 적힌 Site명은 유지됩니다.`))return;const {error}=await supabase.from("sites").delete().eq("id",id);if(error)setMessage(error.message);else loadSites()}
 async function deletePerson(e,id,name){e.preventDefault();if(!confirm(`담당자 '${name}'을 삭제할까요?\n기존 프로젝트의 담당자 표시는 유지됩니다.`))return;const {error}=await supabase.from("personnel").delete().eq("id",id);if(error)setMessage(error.message);else loadPeople()}
 async function rememberPeople(){if(!canCreate)return;for(const [name,department] of [[form.pm,"PM"],[form.design,"설계"],[form.facilityTechnology,"설비기술"],[form.control,"제어"],[form.vision,"비전"]]){const n=name.trim();if(n&&!personnel.some(p=>p.name.toLowerCase()===n.toLowerCase()&&p.department===department))await supabase.from("personnel").insert({name:n,department,created_by:session.user.id})}await loadPeople()}
 function updateMs(i,k,v){setMilestones(r=>r.map((x,n)=>n===i?{...x,[k]:v}:x))}
 async function save(){if(!canEdit)return;if(!editing&&!canCreate)return setMessage("Grade2는 새 프로젝트를 만들 수 없습니다.");if(!form.manufacturingNo.trim()||!form.site||!form.name.trim())return setMessage("제조번호, Site, 프로젝트명은 필수입니다.");if(parse(form.endDate)<parse(form.startDate))return setMessage("종료일을 확인하세요.");const entered=milestones.filter(m=>m.name.trim()&&m.date).map(m=>({...m,name:m.name.trim()}));let error;if(editing&&role==="grade2")({error}=await supabase.from("projects").update({status:form.status,progress:+form.progress||0,auto_progress:form.autoProgress}).eq("id",editing));else{const row={...form,milestones:entered.length?entered:form.milestones,id:editing||uid()};({error}=editing?await supabase.from("projects").update(toDb(row)).eq("id",editing):await supabase.from("projects").insert(toDb(row)))}if(error)return setMessage(error.code==="23505"?"이미 사용 중인 제조번호입니다.":error.message);await rememberPeople();setForm(blank());setEditing(null);setMilestones(rows5());setMessage("저장했습니다.");await loadProjects()}
 function edit(p){if(!canEdit)return;setEditing(p.id);setForm({...p,progress:p.value});const old=(p.milestones||[]).slice(0,5);setMilestones([...old,...rows5().slice(0,5-old.length)]);scrollTo({top:0,behavior:"smooth"})}
 async function remove(id){if(!canDelete||!confirm("프로젝트를 삭제할까요?"))return;const {error}=await supabase.from("projects").delete().eq("id",id);if(error)setMessage(error.message);else loadProjects()}
 async function setRole(id,next){const {error}=await supabase.from("profiles").update({role:next}).eq("id",id);if(error)setMessage(error.message);else loadUsers()}
 function exportExcel(){const esc=v=>`"${String(v??"").replaceAll('"','""')}"`,rows=[["제조번호","Site","프로젝트명","PM 담당자","설계 담당자","설비기술 담당자","제어 담당자","비전 담당자","시작일","종료일","상태","진행률"],...visible.map(p=>[p.manufacturingNo,p.site,p.name,p.pm,p.design,p.facilityTechnology,p.control,p.vision,p.startDate,p.endDate,p.status,`${p.value}%`])];const u=URL.createObjectURL(new Blob(["\ufeff"+rows.map(r=>r.map(esc).join(",")).join("\r\n")],{type:"text/csv"}));const a=document.createElement("a");a.href=u;a.download=`프로젝트_${iso()}.csv`;a.click();URL.revokeObjectURL(u)}
 if(loading)return <div className="center">로그인 확인 중...</div>;if(!session)return <Login/>;if(!profile)return <div className="center">사용자 권한 확인 중... {message}</div>;
 const PersonInput=({label,field,department})=><label>{label}<input list={`p-${department}`} value={form[field]} disabled={role==="grade2"} onChange={e=>setForm({...form,[field]:e.target.value})} placeholder="선택 또는 직접 입력"/><datalist id={`p-${department}`}>{peopleFor(department).map(p=><option key={p.id} value={p.name}/>)}</datalist></label>;
 return <main>
  <header className="main-header"><img className="company-logo" src="/tw-logo.png" alt="(주)TW 로고"/><div className="header-center"><b>TW Project</b><h1>Project Management</h1><p>{session.user.email} · {role}</p></div><div className="header-actions"><button onClick={exportExcel}>Excel 내보내기</button><button onClick={()=>supabase.auth.signOut()}>로그아웃</button></div></header>
  <div className="toolbar-top">{role==="admin"&&<button onClick={()=>setModal("users")}>사용자 권한 관리</button>}{canCreate&&<><button onClick={()=>setModal("sites")}>Site 관리</button><button onClick={()=>setModal("people")}>담당자 관리</button></>}</div>
  {canEdit&&<section><h2>{editing?"프로젝트 수정":"새 프로젝트"}</h2><div className="base-grid"><label>제조번호 *<input value={form.manufacturingNo} disabled={role==="grade2"} onChange={e=>setForm({...form,manufacturingNo:e.target.value})}/></label><label>Site *<select value={form.site} disabled={role==="grade2"} onChange={e=>setForm({...form,site:e.target.value})}><option value="">선택</option>{sites.map(s=><option key={s.id}>{s.name}</option>)}</select></label><label className="wide">프로젝트명 *<input value={form.name} disabled={role==="grade2"} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>시작일<input type="date" value={form.startDate} disabled={role==="grade2"} onChange={e=>setForm({...form,startDate:e.target.value})}/></label><label>종료일<input type="date" value={form.endDate} disabled={role==="grade2"} onChange={e=>setForm({...form,endDate:e.target.value})}/></label><label>상태<select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}>{STATUSES.map(s=><option key={s}>{s}</option>)}</select></label></div><div className="people-row"><PersonInput label="PM 담당자" field="pm" department="PM"/><PersonInput label="설계 담당자" field="design" department="설계"/><PersonInput label="설비기술 담당자" field="facilityTechnology" department="설비기술"/><PersonInput label="제어 담당자" field="control" department="제어"/><PersonInput label="비전 담당자" field="vision" department="비전"/></div><label className="check"><input type="checkbox" checked={form.autoProgress} onChange={e=>setForm({...form,autoProgress:e.target.checked})}/> 자동 진행률</label>{!form.autoProgress&&<input type="range" min="0" max="100" value={form.progress} onChange={e=>setForm({...form,progress:e.target.value})}/>} {canCreate&&<div className="milestone-box"><h3>마일스톤 일괄 입력</h3>{milestones.map((m,i)=><div className="milestone-row" key={m.id}><span>{i+1}</span><input placeholder="마일스톤 이름" value={m.name} onChange={e=>updateMs(i,"name",e.target.value)}/><input type="date" value={m.date} onChange={e=>updateMs(i,"date",e.target.value)}/></div>)}</div>}{message&&<p className="notice">{message}</p>}<button className="primary" onClick={save}>{editing?"수정 저장":"프로젝트 추가"}</button></section>}
  <section><div className="tools"><h2>프로젝트 목록</h2><input placeholder="제조번호, Site, 프로젝트명, 담당자 검색" value={search} onChange={e=>setSearch(e.target.value)}/>{["전체","진행중","완료","지연"].map(x=><button className={filter===x?"active":""} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div><div className="cards">{visible.map(p=><article key={p.id}><div><h3>{p.manufacturingNo||"제조번호 미등록"} · {p.name}</h3><p><b>Site</b> {p.site||"-"}</p><p><b>PM 담당자</b> {p.pm||"-"} · <b>설계 담당자</b> {p.design||"-"} · <b>설비기술 담당자</b> {p.facilityTechnology||"-"} · <b>제어 담당자</b> {p.control||"-"} · <b>비전 담당자</b> {p.vision||"-"}</p><p>{p.startDate} ~ {p.endDate} · {p.status}</p></div><div className="progress"><i style={{width:`${p.value}%`}}/><b>{p.value}%</b></div><div>{canEdit&&<button onClick={()=>edit(p)}>수정</button>}{canDelete&&<button className="danger" onClick={()=>remove(p.id)}>삭제</button>}</div></article>)}</div></section>
  {modal&&<div className="modal-backdrop" onMouseDown={()=>setModal(null)}><div className="modal" onMouseDown={e=>e.stopPropagation()}><button className="modal-close" onClick={()=>setModal(null)}>×</button>{modal==="users"&&<><h2>사용자 권한 관리</h2><div className="manage-list">{users.map(u=><div key={u.id}><span>{u.email}</span><select value={u.role} disabled={u.email==="cmj1012@twgroup.co.kr"} onChange={e=>setRole(u.id,e.target.value)}><option value="admin">관리자</option><option value="grade3">Grade3</option><option value="grade2">Grade2</option><option value="grade1">Grade1</option></select></div>)}</div></>}{modal==="sites"&&<><h2>Site 추가·삭제</h2><div className="add-line"><input placeholder="새 Site명" value={newSite} onChange={e=>setNewSite(e.target.value)}/><button onClick={addSite}>추가</button></div><p className="tip">목록 항목을 마우스 오른쪽 버튼으로 눌러 삭제하세요.</p><div className="tag-list">{sites.map(s=><button key={s.id} onContextMenu={e=>deleteSite(e,s.id,s.name)}>{s.name}</button>)}</div></>}{modal==="people"&&<><h2>담당자 추가·삭제</h2><div className="add-line"><select value={newDept} onChange={e=>setNewDept(e.target.value)}>{DEPTS.map(d=><option key={d}>{d}</option>)}</select><input placeholder="담당자 이름" value={newPerson} onChange={e=>setNewPerson(e.target.value)}/><button onClick={addPerson}>추가</button></div><p className="tip">부서별 목록 항목을 마우스 오른쪽 버튼으로 눌러 삭제하세요.</p>{DEPTS.map(d=><div className="dept-group" key={d}><b>{d}</b><div className="tag-list">{peopleFor(d).map(p=><button key={p.id} onContextMenu={e=>deletePerson(e,p.id,p.name)}>{p.name}</button>)}</div></div>)}</>}</div></div>}
 </main>;
}